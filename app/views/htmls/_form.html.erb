<%= form_for(@html) do |f| %>
  <% if @html.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@html.errors.count, "error") %> prohibited this html from being saved:</h2>

      <ul>
      <% @html.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
